## 一、 项目目标

本项目旨在开发一个基于浏览器的纯前端多周期 RISC-V CPU 模拟与可视化教学工具。系统将包含完整的汇编代码编辑器、运行时控制台以及动态更新的 CPU 数据通路画布。用户可以通过单步运行或连续执行，直观观察指令在 CPU 内部各个阶段的数据流向和控制信号变化。

## 二、 技术选型与渲染方案

1. **基础框架**：采用 React 和 TypeScript 构建，使用 Vite 打包。TypeScript 的强类型约束有利于后续代码的正确性验证。状态管理采用 Zustand。
2. **核心渲染**：采用 SVG 结合 React 组件的方案绘制 CPU 数据通路。由于 CPU 内部属于静态拓扑结构和动态状态叠加，SVG 的声明式特性高度契合此需求，且比直接操作底层的 Canvas 更易于调试和与 React 组件生命周期绑定。
3. **编辑器**：集成 CodeMirror，实现汇编语法的定制高亮。

## 三、 系统架构设计

为了保证系统的高内聚低耦合，架构采用严格的三层分离设计，下层绝不依赖上层：

1. **底层：模拟器引擎层**
   - **定位**：一个纯函数式的有限状态机，负责核心的计算逻辑。
   - **包含模块**：词法语法分析器、汇编器(如果后续需要加入汇编语言)、CPU 核心部件（多路选择器、算术逻辑单元、存储器、寄存器堆）、指令解码器和多周期控制单元。
   - **运行机制**：给定当前状态和时钟信号，产出下一周期的状态。引擎完全不包含任何用户界面的依赖，可独立运行和进行单元测试。
2. **中层：视图映射层**
   - **定位**：引擎层和视图层的翻译枢纽。
   - **运行机制**：接收引擎层生成的周期快照，计算并转换为用户界面可直接消费的视觉属性数据。例如，将引擎中的数据流活动转化为连线的宽度、颜色以及连线上面的数值表现，并负责编排部件变化前后的过渡动画。
3. **上层：视图层**
   - **定位**：负责最终的用户交互和界面渲染。
   - **运行机制**：订阅全局状态并根据配置渲染各个界面面板和 SVG 画布，通过调用底层引擎提供的接口实现界面的响应和运转。

## 四、 核心数据流转机制

1. **快照机制**：引擎每次推进一个时钟周期后，必须生成一个不可变的状态快照。快照是引擎与视图层唯一的通信契约，内部包含了当前周期的所有寄存器值、内存状态、控制信号以及活跃的数据连线信息。通过存储快照序列，系统能够原生支持历史记录的回退与时间旅行调试。
2. **数据驱动拓扑**：CPU 数据通路中的所有部件配置和连线关系不写死在代码中，而是通过标准化的 JSON 结构文件定义。视图层读取配置动态渲染连线图，未来扩展流水线架构时仅需替换配置文件。

## 五、 开发实施路径

项目将严格按照“先纯逻辑后界面呈现”、“先定义接口后编写实现”的顺序进行，分为六个递进阶段，每个阶段的产物必须通过独立的测试验证：

1. **项目初始化与类型定义**：完成基础环境搭建，优先定义架构中所有的类型和接口契约。
2. **汇编器开发**：实现汇编文本到二进制机器码的转换及反汇编功能。
3. **CPU 引擎开发**：开发算术逻辑单元、存储器等独立子模块，最终集成主控制状态机。
4. **映射层开发**：编写逻辑，将引擎状态正确转换为视觉属性。
5. **用户界面开发**：开发基于 SVG 的部件连线以及控制面板界面。
6. **集成调试**：接入状态管理，贯通汇编、模拟执行到图形渲染的完整链路。